import React, { Component } from 'react';

class DropZone extends Component {
    
    render() {
        const {children} = this.props;
        return (
            <div>
                This component is in construction, thanks for downloading
            </div>
        );
    }
}

export default DropZone;